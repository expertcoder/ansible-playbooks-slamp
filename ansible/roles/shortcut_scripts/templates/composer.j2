#!/bin/bash
#Shortcut to run "composer.phar". PHP is been invoked with a huge memory limit as sometimes there is memory problems with composer.
cd /var/www/symfony
php -d memory_limit=2048M /usr/local/bin/composer.phar $@